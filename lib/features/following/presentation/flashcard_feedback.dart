import 'package:flutter/material.dart';

class FlashcardFeedback extends StatelessWidget {
  const FlashcardFeedback({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
